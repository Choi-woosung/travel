<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
			PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
			
<mapper namespace="sSQL">
	
	<!-- 스케쥴리스트가져오기 -->
	<select id="scheduleList" resultType="sVO" parameterType="hashmap">
		SELECT
			s_no sNo, s_name sName, s_sdate sSdate, s_wdate sWdate, s_edate sEdate, s_pic sPic, s_cost sCost
		FROM
			schedule
		<if test="!(sarea eq null) and !(sarea.equals(''))">
			WHERE
				s_area like #{sarea}
		</if>
	</select>
	
	<!-- 리스트 최신순 정렬 -->
	<select id="recentList" resultType="sVO" parameterType="hashmap">
		SELECT
			s_no sNo, s_name sName, s_sdate sSdate, s_edate sEdate, s_wdate sWdate, s_pic sPic, s_cost sCost
		FROM
			schedule
		<if test="!(sarea eq null) and !(sarea.equals(''))">
			WHERE
				s_area like #{sarea}
		</if>
		ORDER BY
			s_wdate desc
	</select>
	
	<select id="schedule" resultType="sVO">
		SELECT
			s_name sName, s_sdate sSdate, s_area sArea, s_edate sEdate, s_pic sPic, s_body sBody, s_cost sCost, s_rate sRate
		FROM
			schedule
	</select>
	
</mapper> 