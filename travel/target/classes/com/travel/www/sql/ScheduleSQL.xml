<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
			PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
			
<mapper namespace="sSQL"> 
	
	<!-- 스케쥴리스트가져오기 -->
	<select id="scheduleList" resultType="sVO">
		SELECT
			s_name sName, s_sdate sSdate, s_edate sEdate, s_pic sPic
		FROM
			schedule
	</select>
	
		<select id="schedule" resultType="sVO">
		SELECT
			s_name sName, s_sdate sSdate, s_area sArea, s_edate sEdate, s_pic sPic, s_body sBody, s_cost sCost, s_rate sRate
		FROM
			schedule
	</select>
	
</mapper>